// Filled automatically after the shared Supabase backend is deployed.
// Leave blank to keep the current local-cache fallback.
window.SARA_SHARED_RESEARCH_URL = window.SARA_SHARED_RESEARCH_URL || '';
